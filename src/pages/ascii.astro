---
const posts = [
    {
        date: "2025-07-20",
        title: "Abstraction vs Duplication",
        description: `Learn more about <a href="https://clarity.dev">clarity</a> and <a href="https://duplication.dev">duplication</a>.`
    },
    {
        date: "2025-07-15",
        title: "Migrating Blogs to Astro",
        description: `Lessons from transitioning to <a href="https://astro.build">Astro</a>.`
    },
    {
        date: "2025-07-01",
        title: "Embedding SPAs in Markdown",
        description: `Mixing static Markdown with dynamic <a href="https://example.com/spa">SPA islands</a>.`
    }
];

const footerLinks = ["about", "contact", "projects", "exit"];
---
<style>
    html, body {
        margin: 0;
        padding: 0;
        background-color: #0a0a0a;
        color: #00ff00;
        font-family: "Fira Code", monospace;
        min-width: 80ch;
        min-height: 100vh;
    }

    main {
        padding: 2rem;
        width: 80ch;
        margin: 0 auto;
    }

    h1 {
        font-size: 20px;
        font-weight: bold;
        background: linear-gradient(to right, #00ff00, #66ff66);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        color: transparent;
        margin-bottom: 2rem;
    }

    .post {
        margin-bottom: 1.5rem;
    }

    .description {
        margin-top: 0.2rem;
    }

    a {
        color: #00ffaa;
        text-decoration: underline;
    }

    footer {
        border-top: 1px solid #004400;
        margin-top: 3rem;
        padding-top: 1rem;
        display: flex;
        justify-content: space-between;
    }

    ::selection {
        background: #004400;
        color: #00ff00;
    }
</style>

<main>
    <h1>jigar.gosar:~$</h1>

    {posts.map(post => (
            <div class="post">
                <div>[{post.date}] {post.title}</div>
                <div class="description" set:html={post.description}></div>
            </div>
    ))}

    <footer>
        {footerLinks.map(text => (
                <a href="#">{`[${text}]`}</a>
        ))}
    </footer>
</main>
